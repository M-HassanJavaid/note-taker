import React, { useState } from 'react'
import Loader from '../components/Loader';


const Dashboard = () => {

    // const [isLoading, setIsLoading] = useState(true);
    // async function get(params) {
        
    // }

    // if (isLoading) {
    //     return <Loader/>
    // }

  return (
    <main>
        Dashborad is in development
    </main>
  )
}

export default Dashboard